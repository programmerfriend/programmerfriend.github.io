version: "3"

services:
  jekyll:
    image: jekyll/jekyll
    ports:
      - "4000:4000"
    command: jekyll serve --watch --incremental --host "0.0.0.0" --config _config.yml
    volumes:
      - .:/srv/jekyll
